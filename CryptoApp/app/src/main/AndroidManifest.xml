<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cm.homework.cryptoapp">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomWordSample">

        <activity
            android:name="cm.homework.cryptoapp.activities.NewCoinActivity"
            android:exported="false"
            android:label="Create new coin"/>
        <activity
            android:name="cm.homework.cryptoapp.activities.CoinActivity"
            android:exported="false"
            android:label="Coin information"/>
        <activity
            android:name="cm.homework.cryptoapp.activities.MainActivity"
            android:exported="true"
            android:label="Crypto ">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>